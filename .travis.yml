language: python
python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4
install:
  - pip install coveralls
script:
  - nosetests --with-coverage
after_success:
  - coveralls
matrix:
  allow_failures:
    - python: 3.3
    - python: 3.4
  fast_finish: true
